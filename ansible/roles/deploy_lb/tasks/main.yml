---
-   name: Install packages
    apt:
        name: "{{ packages }}"
        state: present

-   name: Check if certificate already exists.
    stat:
        path: /etc/letsencrypt/live/{{ domain_name }}/cert.pem
    register: letsencrypt_cert

-   name: debug msg to show if cert exist
    debug: msg="{{ letsencrypt_cert.stat.exists }}"

-   name: Stop services to allow certbot to generate a cert.
    service:
      name: nginx
      state: stopped
    when: not letsencrypt_cert.stat.exists

# Let's Encrypt certificate (may hit rate limits)
-   name: Generate new certificate if one doesn't exist.
    when: not letsencrypt_cert.stat.exists
    command:
      cmd: "certbot certonly --standalone --noninteractive --expand --agree-tos --email {{ admin_email }} -d {{ domain_name }} -d www.{{ domain_name }}"
    ignore_errors: true

# Self-signed certificate (no rate limits, comment out Let's Encrypt task above to use this)
# -   name: Generate self-signed certificate if one doesn't exist.
#     when: not letsencrypt_cert.stat.exists
#     shell: |
#       mkdir -p /etc/letsencrypt/live/{{ domain_name }}
#       openssl req -x509 -nodes -days 365 -newkey rsa:2048 \
#         -keyout /etc/letsencrypt/live/{{ domain_name }}/privkey.pem \
#         -out /etc/letsencrypt/live/{{ domain_name }}/cert.pem \
#         -subj "/C=SE/ST=Blekinge/L=Karlskrona/O=BTH/CN={{ domain_name }}" \
#         -addext "subjectAltName=DNS:{{ domain_name }},DNS:www.{{ domain_name }}"
#       # Create fullchain.pem (required by nginx)
#       cp /etc/letsencrypt/live/{{ domain_name }}/cert.pem /etc/letsencrypt/live/{{ domain_name }}/fullchain.pem

-   name: Remove default conf
    file:
        state: absent
        path: /etc/nginx/sites-enabled/default

-   name: Template nginx config to server
    copy:
        src: nginx.conf
        dest: /etc/nginx/nginx.conf
        mode: "644"


-   name: Template nginx site config to server
    template:
        src: load-balancer.conf.j2
        dest: /etc/nginx/sites-available/load-balancer.conf
        mode: "644"
    notify:
    - Restart nginx

-   name: Link site to enabled
    file:
        state: link
        force: yes
        src: /etc/nginx/sites-available/load-balancer.conf
        path: /etc/nginx/sites-enabled/load-balancer.conf
    notify:
    - Restart nginx

-   name: Start services after cert has been generated.
    service:
      name: nginx
      state: started
    when: not letsencrypt_cert.stat.exists