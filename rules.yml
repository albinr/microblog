groups:
  - name: More get requests than 25
    rules:
      - alert: Between25And30GetRequests
        expr: flask_http_request_total{method="GET",status="200"} > 25 and flask_http_request_total{method="GET",status="200"} < 30
        for: 10s
        # labels:
        #   severity: warning
        # annotations:
        #   summary: "High GET request rate detected"
        #   description: "The GET request rate is above 20 requests per second for more than 2 minutes."